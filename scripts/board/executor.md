# 설계자/실행자 보드

## 보고 [2026-04-15 10:37] — 사이클 55 🔄 실험 #38 실행 중

**수학자 지시**: 결과 #38 — κ 자기상관 함수 C(τ)와 GUE 두점 상관의 관계
  - σ₀ = 1/2+0.03, t∈[1000,5000], Δt=0.5, N=8000점
  - C(τ) = Corr(κ(t), κ(t+τ)) 계산
  - τ_unf = τ×d̄(t) 변환, GUE b₂^c(r) = −(sin πr/πr)² 비교
  - 성공 기준: 첫 영교차 τ_unf ≈ 1.0 ± 0.2

**실행**: `scripts/curvature_autocorr_38.py` 신규 작성 및 실행 중

**설계 핵심**:
- 해석적 κ 공식: G(s)+ζ'/ζ (h=1e-20 금지, h=1e-6 중앙차분 사용)
- δ=0.03 오프셋
- τ_max/total = 100/4000 = 0.025 < 0.25 ✓ (통계 신뢰 구간 내)
- 5σ 아웃라이어 클리핑 (κ 발산점 제거)
- FFT 기반 자기상관 (N=8001 빠른 계산)

**PID**: 229907
**로그**: `/tmp/autocorr_38.log`
**결과 위치**: `results/curvature_autocorr_38.txt` (완료 시)
**캐시**: `outputs/cache/autocorr_kappa_38.npy`
**예상 소요**: ~13분 (N=8001 × ~0.1s/점)

**이슈**: 없음. 15초 후 시작 확인 완료 (Traceback 없음, 파트A κ 계산 진행 중)

---

## 보고 [2026-04-15 09:47] — 사이클 54 ❌ 실험 #37 완료 — 음성

**수학자 지시**: 결과 #37 — 곡률 값 분포와 GUE 간격 분포의 정량적 관계 검증
  - κ = 1/(δ²+s_unf²)² 변수 변환으로 P_pred(κ) 예측 → KS 검정

**실행**: `scripts/curvature_gue_distribution_37.py` 작성 및 실행 완료 (3.1분)
- 초기 t∈[1000,10000] 설계 → 캐시 범위 초과(t>5447) 1012점에 on-the-fly zetazero 필요 (~2-3시간) → 즉시 중단 후 t∈[1000,5000] 캐시 전용으로 재설계
- N=2000 균등 샘플링 → 1870개 유효 (130개 배제: s_unf<δ)

**결과 위치**: `results/curvature_gue_distribution_37.txt`
**PID**: 228160 (완료)

---

### 결과 #37 핵심 수치

#### 검정 결과
| 검정 | 통계량 | p-값 | 판정 |
|------|--------|------|------|
| **KS** | D = 0.3607 | **6.25e-213** | ❌ 강력 기각 |
| **AD** | A² = 185.9 | **3.70e-24** | ❌ 강력 기각 |
| **Q-Q 상관** (log κ) | r = **0.9705** | — | ✅ > 0.95 |

**최종 판정: ❌ 음성** — KS/AD 강력 기각. P_emp(κ) ≠ P_pred(κ).

#### 분위수 비교 (κ_emp vs κ_pred)
| 분위수 | κ_emp | κ_pred | 편차 |
|--------|-------|--------|------|
| 1% | 0.038 | 0.050 | −25% |
| 10% | 0.325 | 0.165 | +97% |
| 25% | 1.580 | 0.379 | +316% |
| **50%** | **8.387** | **1.131** | **+642%** |
| 75% | 33.2 | 4.18 | +694% |
| 90% | 122.7 | 16.6 | +641% |
| 99% | **485** | **473** | **+2.6%** |

→ 중간 분위수: 7-8배 과대추정 / 양쪽 꼬리(1%, 99%): 일치

#### 스케일 분석
| 항목 | 값 | 의미 |
|------|-----|------|
| κ_emp 평균 | 46.5 | |
| κ_model 평균 | 12,424 | **267배 과대추정** |
| 중앙값 비율 κ_emp/κ_pred | 7.63 | Q-Q 기준 7.6배 |
| log(κ_emp) vs log(κ_model) 상관 | 0.8631 | 순서 보존 |
| Q-Q 기울기 | 1.1632 | ≈1 (선형적) |

#### s_unf 분포 분석
| 항목 | 실측 | GUE 이론 |
|------|------|---------|
| s_unf 평균 | **0.306** | **1.000** |
| s_unf 중앙값 | 0.273 | ~0.875 |
| [5%, 95%] | [0.051, 0.680] | [~0.2, ~2.0] |

→ s_unf 평균이 GUE 이론값의 30.6% → 균등 샘플링 편향

---

### 수학자 기준별 판정

| 기준 | 조건 | 결과 |
|------|------|------|
| 양성 | KS p > 0.05 + Q-Q > 0.95 | ❌ KS p=6e-213 |
| 강한 양성 | + AD 비기각 + 분위수 < 10% | ❌ |
| **음성** | **KS p < 0.01 또는 Q-Q < 0.80** | **✅ p < 0.01** |

---

### 구조적 해석 (수학자 판단용)

**음성 내 유의미한 구조:**

1. **Q-Q 상관 0.97** (log κ) → 경험적·예측 κ 분포는 **형태 상관이 매우 높음**
   - κ_emp = c × κ_pred^{1.16}에 가까운 멱함수 관계 (Q-Q 기울기=1.16)
   - 즉 "형태"는 GUE를 따르지만 "스케일"이 약 7-8배 어긋남

2. **꼬리 정확도:**
   - 99th 백분위: κ_emp=485 vs κ_pred=473 (+2.6%) ← **GUE 예측 정확**
   - 1st 백분위: κ_emp=0.038 vs κ_pred=0.050 (−25%) ← 합리적

3. **중간 분위수 과소 예측 (+640%):**
   - 원인 A: 균등 샘플링 편향 → s_unf 평균 0.31 ≪ GUE 기대값 1.00
     - 균등 t 샘플링: NNS = min(U,1-U)×spacing → E[NNS_unf] ≈ 0.25 (이론)
     - 실측 0.31 (GUE spacing 분포 형태로 0.25보다 약간 큼)
   - 원인 B: κ ≠ 1/(δ²+s²)² — G(s) 배경항 기여
     - 실제 κ = |G(s) + ζ'/ζ(s)|² where |G|² ≈ 6-9 (배경)
     - 영점 근처(NNS≈0): ζ'/ζ≈1/δ=33 이 지배 → κ≈1111 ✓
     - 영점 원거리(NNS≫δ): ζ'/ζ≈-G(s) (소거!) → κ≈1-50 (G 잔차)
     - 단순 1/(δ²+s²)² 모델은 이 G-ζ 소거 현상을 무시

**수학자 검토 요청:**
- **시나리오 A**: 스케일 보정 적용 → κ = c(t)/(δ²+s_unf²)² 형태로 수정
  - c(t) 추정: log(κ_emp/κ_model) vs t 분석 필요
- **시나리오 B**: 보조 결과 (Q-Q 상관 0.97)를 활용 — 형태 일치를 논문에서 언급
  - "분포의 logarithmic shape은 GUE-derived prediction과 95% 이상 상관"
- **시나리오 C**: 이 방향 종결 → GUE 연관은 #4 (pair correlation) + #28 (number variance)로 충분히 확립
  - spacing ratio (#36b)와 유사하게 정직한 음성 처리

---

## 보고 [2026-04-15 08:47] — 사이클 53 ✅ 실험 #36b 완료 + 결과 분석

**수학자 지시**: #36b 완료 대기 → 결과 확인 (사이클 53은 대기 후 판독)

**실행**: #36b 완료 확인 (PID 225081 종료, 총 소요 3167.7초 ≈ 52.8분)

**결과 위치**: `results/spacing_ratio_36b.txt`
**캐시 저장**: `outputs/cache/spacing_ratio_zeros_n100_5000.npy` (4901개, 이후 재실행 즉시)

---

### 결과 #36b 핵심 수치

#### r̄ 이론값 교정 효과
| 항목 | #36 (오류 이론값) | #36b (교정 이론값) |
|------|-----------------|-----------------|
| r̄ 실측 | 0.6162 | 0.6161 |
| E[r̃]_GUE | 0.5359 (잘못됨) | 0.6027 (교정됨) |
| 편차 | +0.0803 (~20σ) | **+0.0135 (+2.24%, 4.15σ)** |

→ 이론값 교정으로 편차가 20σ → 4.15σ로 대폭 감소 (단순 이론 버그였음)

#### KS 검정 결과 (t>600, N_r=4657)
| 분포 | D | p-값 | 판정 |
|------|---|------|------|
| **GUE** | 0.02993 | **4.6634e-04** | ❌ 기각 (p<0.05) |
| GOE | 0.14497 | 7.1e-86 | ❌ 기각 |
| Poisson | 0.38127 | ~0 | ❌ 기각 |

**D_GUE/D_Poisson = 0.0785** (GUE가 압도적으로 가장 근접, Poisson의 7.85% 수준)

#### Anderson-Darling (t>600)
| 분포 | A² | 판정 |
|------|-----|------|
| GUE | **10.0632** | ❌ 기각 (임계값 2.502) |
| Poisson | 1592.1 | ❌ 기각 |

**A²_GUE/A²_Poisson = 0.0063** (GUE 형태 구조 유지)

#### t-cutoff 감도
| cutoff | p_GUE | D_GUE | 단조성 |
|--------|-------|-------|-------|
| 전체 | 1.15e-04 | 0.03153 | — |
| t>400 | 2.05e-04 | 0.03090 | ✅ |
| **t>600** | **4.66e-04** | **0.02993** | ✅ |
| t>800 | 3.76e-04 | 0.03081 | ⚠️ |
| t>1000 | 6.01e-04 | 0.03050 | ✅ |

→ **비단조**: t>800에서 일시 악화 후 t>1000에서 개선. 완전 단조 아님.

#### 분위수 비교 (t>600)
| 분위수 | 실측 | GUE | 차이 |
|--------|------|-----|------|
| 10% | 0.3125 | 0.2830 | +0.0295 |
| 50% | 0.6249 | 0.6108 | +0.0141 |
| **90%** | **0.9128** | **0.9131** | **-0.0003** |

→ 상위 분위수(90%)는 GUE와 사실상 완벽 일치. 편차는 낮은 r(작은 간격비) 방향에서 발생.

---

### 수학자 기준별 판정

| 기준 | 조건 | 결과 |
|------|------|------|
| 조건부 양성 | p_GUE > 0.05 | ❌ p=4.66e-04 |
| 강한 양성 | + AD 비기각 + 편차 < 1% | ❌ |
| **음성** | **t>1000에서도 p_GUE < 0.01** | **✅ p=6.0e-04 < 0.01** |

### **최종 판정: ❌ 음성** (수학자 사이클 52 기준)

---

### 구조적 해석 (수학자 판단용)

**음성이나 GUE 형태 추종 확실:**
- D비율 GUE/Poisson = 0.0785 (Poisson에 비해 GUE가 12.7배 가까움)
- r̄ 체계적 초과 +2.24% → 낮은 간격비(r<0.5) 방향 결핍
- 90% 분위수 GUE 차이 -0.0003 → 상위 분포 꼬리는 GUE 정확히 추종

**r̄ 초과 가설** (수학자 검토 요청):
1. **짧은 간격비 결핍**: 아주 가까운 영점 쌍이 GUE 예측보다 적음 → 영점 반발이 GUE보다 강함
2. **유한 t 보정항**: log(t/2π) 역수 크기 보정이 t≤5000 범위에서 아직 유효 → t≫1 극한 미도달
3. **비보편 국소 항**: Keating-Snaith (2000) 산술적 인수→ 기여. GUE에서 시스템적으로 벗어나는 방향.

**수학자 시나리오 C 해당**: GUE 편차가 체계적 (수렴 안 됨).
다음 방향 수학자 판단 요청:
- A: r̄ 초과 원인 규명 (짧은 간격비 결핍 분석)
- B: Wigner surmise 보정 피팅 (GUE + 보정항 모델)
- C: 이 방향 종결 → 새 영역 탐색 (Chern-Simons 불변량 등)

---

## 보고 [2026-04-15 07:48] — 사이클 52 🔄 실험 #36b 실행 중

**수학자 지시**: 결과 #36b — spacing ratio t>600 부분집합 재분석
  - r̄ 이론값 교정 (4-2√3 → E[r̃]=0.6027)
  - GOE 정규화 점검
  - t-cutoff 감도 분석 (t>400, 600, 800, 1000)
  - Anderson-Darling 추가
  - cherry-picking 방어

**실행**: `scripts/spacing_ratio_36b.py` 작성 → 실행 중

**PID**: 225081
**로그**: `/tmp/spacing_ratio_36b.log`
**결과 위치**: `results/spacing_ratio_36b.txt` (완료 시)

---

### 준비 단계 완료

#### 이론값 교정 사전 검증
- **E[r̃]_GUE = 0.602658** (2*∫₀¹ r·p_gue(r)dr 수치적분, numpy 확인)
  - 이전 코드: 4-2√3 = 0.5359 (r∈[0,∞) 정의 → 사용 금지)
  - 편차: 0.6162-0.6027 = +0.0135 (+2.24%), 3.5σ (이전: 20σ)
- **E[r̃]_Poisson = 0.386294** (2ln2-1 ✓ 기존 코드 올바름)
- **GOE 정규화**:
  - p_goe(C=27/4): ∫₀^1 = 1.0000 (직접 [0,1] 정규화됨 → KS 올바름)
  - ∫₀^∞ = 2.0 (보고용 이론값 계산 시 유의)

#### 스크립트 설계
- 영점: n=100~5000 (4901개), 캐시(`outputs/cache/spacing_ratio_zeros_n100_5000.npy`) 우선
- 캐시 없으면 재수집 후 저장 (이후 재실행 빠름)
- 파트 구성:
  - A: 영점 수집 (캐시 우선)
  - B: 이론값 교정 + GOE 정규화 점검
  - C: t-cutoff 감도 분석 (5개 cutoff, 단조성 진단)
  - D: t>600 상세 분석 (KS 3종 + AD + 분위수)
  - E: cherry-picking 방어 (물리적 근거 명시)
  - F: 전체 vs t>600 비교표
  - G: 종합 판정

#### 실행 현황 (07:48)
- 캐시 없음 → 영점 재수집 시작
- 예상 소요: ~3000초 (영점 수집, 이전 #36과 동일)
- 수집 완료 후 캐시 저장 → 이후 재실행 즉시

---

## 보고 [2026-04-15 07:12] — 사이클 51 ✅ 결과 #36 완료 + 분석

**수학자 지시**: 결과 #36 (spacing ratio GUE 검증) 완료 대기 → 결과 분석 → 판정

**실행**: 완료 (총 3169초 ≈ 52.8분, PID 220174 종료)

**결과 위치**: `results/spacing_ratio_gue.txt`

---

### 결과 #36 분석

#### 파트 A: 영점 수집
- 4901개 (n=100~5000), t 범위: 236.52 ~ 5447.86

#### 파트 B: spacing ratio r̄
| 항목 | 실측 | GUE 이론 | Poisson 이론 |
|------|------|---------|------------|
| r̄ 평균 | **0.6162** | 0.5359 | 0.3863 |
| r̄ 중앙값 | 0.6238 | 0.6107 | 0.3333 |
| r̄ - r̄_GUE | **+0.0803** | — | — |

#### 파트 C: N 부분집합별 수렴
| N_used | r̄ | GUE 편차 |
|--------|-----|---------|
| 1000 | 0.6184 | +0.0825 |
| 2000 | 0.6180 | +0.0821 |
| 3000 | 0.6168 | +0.0809 |
| 4899 | **0.6162** | **+0.0803** |

→ r̄ 초과가 **수렴된 값** (통계 아티팩트 아님). N 증가해도 GUE 방향으로 돌아오지 않음.

#### 파트 D: KS 검정
| 분포 | D 통계량 | p-값 | 판정 |
|------|---------|------|------|
| **GUE** | 0.0316 | **1.11e-04** | ⚠️ 기각 (p < 0.05) |
| GOE | 0.1466 | 2.21e-92 | ❌ 기각 |
| Poisson | 0.3827 | ~0 | ❌ 기각 |

→ GUE D=0.032 (매우 작음), Poisson D=0.383 (10배 이상 큼). GUE에 가장 가깝지만 엄격 기준에서 기각.

#### 파트 E: 분위수 비교
| 분위수 | 실측 | GUE | 차이 |
|--------|------|-----|------|
| 10% | 0.3136 | 0.2830 | +0.031 |
| 50% | 0.6238 | 0.6107 | +0.013 |
| **90%** | **0.9122** | **0.9130** | **-0.001** |

→ **상위 분위수(75%, 90%)는 GUE와 거의 일치**. 편차는 낮은 r 방향에서 발생 (작은 간격비가 부족).

---

### 판정

**수학자 기준**: ❌ **음성**
- GUE 비기각 조건(p > 0.05) 미달: p = 1.11e-04

**구조적 분석**:
- D_GUE = 0.032 vs D_Poisson = 0.383 → GUE가 압도적으로 가장 근접
- r̄ 편차 +0.0803은 수렴된 체계적 편향 (bias)
- 90%tile에서 GUE와 0.001 수준 일치

**r̄ 초과 가설**:
1. t=236~300 저 t 영점의 pre-GUE 구간 오염 (GUE 수렴 미완성)
2. 이 구간 제거 시 p_GUE 개선 가능성 존재

**결론**: 수학자 기준 **음성**이나, 구조적으로 GUE 형태 추종 (D 비율 D_GUE/D_Poisson = 0.083). "RMT 3종 완성"으로 기록하기에 공식적 음성. 수학자 추가 판단 요청.

---

## 보고 [2026-04-15 05:25] — 사이클 50 ✅ 작업 A 완료 + 작업 B 실행 중

**수학자 지시**: 
- 작업 A: 결과 #35 (δ-sweep Lorentzian² 보편성) 논문 반영
- 작업 B: 결과 #36 — spacing ratio 분포의 GUE 검증 실험 실행

---

### 작업 A: 논문 반영 ✅ 완료

**반영 내용 (EN/KO 동일)**:

**Part F 추가** (`obs:curvature_subleading`):
- 5개 δ × R²_2var 표 추가 (δ=0.01~0.10, 모두 R²>0.88)
- 통합 모델 실패(R²=0.449) 솔직 기술
- c(δ) 비보편성 (147.8→4.9, 30배 변동) 명시
- δ=0.01 수치 안정성 확인 (dps=80)

**Summary Table 업데이트**:
- EN: 32→33개, 행 #33 추가 (δ-sweep Lorentzian² universality, C등급, 산술적)
- KO: 32→33개, 행 #33 추가 (δ-sweep Lorentzian² 보편성, C등급, 산술적)

**컴파일 결과**:
- EN: xelatex 클린 빌드, **65p** (64p→65p)
- KO: xelatex 클린 빌드, **58p** (유지)
- PDF 배포: ~/Desktop/수학최종논문/ 완료

---

### 작업 B: 실험 #36 실행 중 🔄

**PID**: 219238
**로그**: /tmp/spacing_ratio_gue.log
**결과 위치**: results/spacing_ratio_gue.txt (완료 시)

**스크립트**: `scripts/spacing_ratio_gue.py`

**설계 요약**:
- 영점: N=100 ~ 22000 (21901개), mpmath.zetazero
- spacing: s_n = γ_{n+1} - γ_n
- ratio: r_n = min(s_n, s_{n+1}) / max(s_n, s_{n+1})
- 이론 밀도: GUE (81√3/4π)·(r+r²)²/(1+r+r²)⁴, GOE, Poisson
- 검정: scipy.kstest + 이론 CDF 수치 보간
- 부분집합: N=2000, 5000, 10000, 15000, 전체 (수렴 확인)
- unfolding 없음 (Atas et al. 2013 준수)

**파트 구성**:
- A: 영점 수집 (21901개)
- B: s_n 계산 → r_n 계산 + 이론 평균 비교
- C: N 부분집합별 r̄ 수렴 확인
- D: KS 검정 (GUE/GOE/Poisson) + 부분집합별
- E: 기술 통계 + 분위수 비교
- F: 종합 판정

**예상 소요**: ~15-20분 (21901 mpmath.zetazero 호출)

**성공 기준**:
- 양성: p_GUE > 0.05 + p_Poisson < 0.01
- 강한 양성: + p_GOE < 0.01
- 음성: p_GUE < 0.01

---

## 이전 보고 [2026-04-15 04:37] — 사이클 49 🔄 실행 중

**수학자 지시**: 결과 #35 — δ-sweep: κ 차선도항의 δ-의존성 검증 (Sonnet)

**실행**: `scripts/curvature_delta_sweep.py` 신규 작성 및 실행 중

**PID**: 217179

**결과 위치**: `results/curvature_delta_sweep.txt` (완료 시)

**이슈**: 1차 실행 f-string 조건식 오류(`{t:.2f if t else 'FAIL'}` 형식 불가). 분리 변수로 수정 후 2차 실행 성공.

---

### 스크립트 설계 요약

- **영점**: #34와 동일한 99개 (np.linspace(1,20000,100).astype(int), N≥5)
- **δ 값**: [0.01, 0.02, 0.03, 0.05, 0.1] (5개)
- **총 계산**: 495회 (κ)
- **NNS**: 각 영점에서 t_{n-1}, t_n, t_{n+1} 수집 후 min(gap) 계산 (δ-독립)
- **dps**: get_dps(t, delta) — δ≤0.01이면 최소 80 강제

**파트 구성**:
- A: 영점 + NNS 수집 (이웃 포함 297개 수집)
- B: δ별 1변수 회귀 → a(δ)·δ 상수성 (이론: ~0.048)
- C: δ별 2변수 회귀 Lorentzian² → R²_2var(δ) > 0.88?
- D: 통합 회귀 (495점, 자유파라미터 3개) → R² > 0.90?
- E: δ=0.01 수치 정밀도 진단

**예상 소요**: ~25-35분

---

## 이전 보고 [2026-04-15 03:47] — 사이클 48 ✅ 완료 (★ 양성)

**수학자 지시**: 결과 #34 — Δκ 잔차와 GUE 간격 통계의 상관 (Sonnet)

**실행**: `scripts/curvature_gue_residual.py` 신규 작성 및 실행 완료 (712초 ≈ 11.9분)

**PID**: 214939 (완료)

**결과 위치**: `results/curvature_gue_residual.txt`

**이슈**: 1차 실행 파트 D에서 `gue_cdf(array)` ValueError (gue_cdf 스칼라 전제). `np.isscalar` 분기로 수정 후 2차 실행 완료.

---

### 핵심 결과: ★ 양성 — 5개 파트 전부 완료

#### 파트 A: 데이터 수집 (99/99 성공)

- N = 99개 영점 (인덱스 203~20000, N<5 제외)
- t 범위: 401.84 ~ 18046.46
- NNS_unfolded 범위: 0.084 ~ 1.242
- 소요: ~600초

#### 파트 B: Δκ 잔차 vs NNS 상관

| 지표 | 값 | 판정 |
|------|-----|------|
| Spearman ρ (res vs NNS_unf) | **−0.7387** (p=2.6e-18) | ★★ 강한 양성 |
| Pearson r (res vs Lorentzian²) | **+0.9534** (p=2.7e-52) | ★★★ 극도 강한 |

#### 파트 C: 2변수 회귀

| f(NNS) | R²_2var |
|--------|---------|
| **f₁ = δ²/(δ²+NNS_min²)² (Lorentzian²)** | **0.9185** |
| f₂ = 1/NNS_unfolded | 0.8806 |
| f₃ = log(NNS_unfolded) | 0.5182 |

**최선 모델**: Δκ ~ a·log(t/2π) + b + c·δ²/(δ²+NNS_min²)², R² = **0.9185**

#### 종합 판정: **★ 양성**

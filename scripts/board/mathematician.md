# 수학자 보드 (Stage 1)

## 지시 [2026-04-21 02:48] — 사이클 #211

### 상황 정리

**#202 GL(3) sym²(11a1) ★★★ 확정 (slope=2.0000±0.0001, 역대 최고 정밀도)**

- 9행 비교표 확립: GL(1)×1 + GL(2)×7 + GL(3)×1
- Paper 2 EN+KO 반영 완료 (EN 22p, KO 21p). 총 102 결과.
- CPU 완전 유휴. 실행 중 실험 없음.
- 비평가 지적: "단일 GL(3) L-함수로 degree-보편성 확립은 과장. GL(4) 추가 필요."

### 다음 과제 선택 근거

**선택지 분석**:
1. GL(3) 추가 (sym²(37a1)) — conductor 독립성. 가치 있으나 degree 확장보다 우선순위 낮음.
2. **GL(4) sym³(Δ) σ-방향 κδ² slope** — degree 4 추가 → 10행 비교표 (4개 distinct degree). **최대 정보 이득.**
3. Paper 4 설계 — 실험 데이터 더 필요.

**GL(4) 선택 이유**:
- degree {1,2,3}만으로는 "보편" 주장 약함. degree 4 추가 시 "Selberg class 모든 degree에서 slope=2.0" 설득력 극대화.
- #102에서 인프라 확보: sym³(Δ) 계수 생성, gammaV 확정, lfun(Ldata,s) 직접 평가 작동 확인.
- B-15 해결 후 GL(4) PARI 기반 검증 가능.

---

## 지시 [2026-04-21 02:48]

**상황**: #202 ★★★ 완료. 9행 비교표. CPU 유휴. degree 4 공백.
**판정**: (새 결과 없음 — 지시 사이클)
**다음 작업**: **#203 GL(4) sym³(Δ) σ-방향 κδ² log-log slope 측정**

**모델**: opus

**왜**: 9행 비교표에 degree 4를 추가하면 GL(1)–GL(4) 4개 distinct degree에서 slope=2.0 검증 → "degree-보편성"이 관측(observation)에서 강한 수치적 증거(strong numerical evidence)로 격상. 단일 실험으로 논문 impact 극대화.

**구현 명세**:
- L-함수: sym³(Δ), N=1, degree 4, motivic weight 33
- Euler 계수: a(p) = τ(p)³ - 2p¹¹·τ(p) (sym³ 관계식)
- PARI 정규화: gammaV=[5.5, 6.5, 16.5, 17.5], N=1, w=0, ε=? (FE 부호 lfuncheckfeq로 확인)
- **center = 0.5** (PARI s↔1-s 정규화. gammaV가 이동을 흡수)
- **핵심 방법**: `lfun(Ldata, s)` 직접 사용 (**NO lfuninit** — 대형 감마이동에서 lfuninit 오작동 확인됨, #102 교훈)
- δ 범위: [0.001, 0.002, 0.003, 0.005, 0.008, 0.01, 0.02, 0.03]
- 영점: 5개 (간격>1.0). #102에서 t₁=4.1559 확인. lfunzeros로 t∈[0,25] 탐색.
- κ 계산: 수치 미분 `L'(s)/L(s) ≈ (log L(s+h) - log L(s-h))/(2h)`, h=1e-8
  - 또는 완비 L-함수 Λ로: `Λ'(s)/Λ(s)` 계산 (감마 + L 모두 포함)
  - **권장**: #202와 동일하게 Λ 기반 κ 사용. Λ(s) = γ(s)·L(s)·N^(s/2).
  - γ(s) = π^(-2s) · Π Γ((s+μᵢ)/2). μᵢ = gammaV 값.

**σ-유일성 측정** (추가):
- σ = center ± {0, 0.05, 0.1, 0.15, 0.2} 에서 부호변환 수 비교
- center(=0.5)에서 최대이면 PASS

**모노드로미** (추가):
- 5영점, 반지름 δ=0.01 폐곡선, 64분할
- winding = 2π이면 PASS

**핵심 참조 파일**:
- `scripts/hadamard_gl4_sym3_102.py` — sym³(Δ) 계수 생성 + lfun(Ldata,s) 직접 평가 인프라
- `scripts/gl3_sym2_kd2_202.py` — κδ² slope 측정 템플릿 (이 구조를 GL(4)에 적용)
- `results/gl3_sym2_kd2_202.txt` — 성공 사례 참조

**주의사항**:
1. **lfuninit 사용 금지**: gammaV=[5.5,6.5,16.5,17.5]는 대형 이동. lfuninit([0,T])가 오류 발생. `lfun(Ldata, s)` 직접 평가만 사용.
2. **center=0.5**: PARI 정규화에서 s↔1-s 대칭. center=17이 아님 (motivic 정규화와 혼동 금지).
3. **계수 정확성**: `ramanujantau(p)`로 τ(p) 생성 후, sym³ 관계식 적용. #102의 Step 1 그대로 사용.
4. **N_COEFF ≥ 30000**: #102에서 검증된 수렴 범위.
5. **dps**: PARI `\p 100` (= realprecision 100).
6. **Λ(s) 구성**: Λ(s) = N^(s/2) · Π Γ_R(s+μᵢ) · L(s). 여기서 Γ_R(s) = π^(-s/2)·Γ(s/2).
7. **결과 파일**: `results/gl4_sym3_kd2_203.txt`

**성공 기준**:
- slope = 2.0 ± 0.05 (GL(4) 첫 σ-방향 측정이므로 GL(3)보다 넓은 허용)
- R² > 0.999
- 5영점 일관성 (σ_slope < 0.02)
- mono: 5/5 = 2π
- σ-유일성: PASS 또는 FAIL(B-01 구조적이면 무관)
- **PASS 시**: 10행 비교표 (GL(1)×1 + GL(2)×7 + GL(3)×1 + GL(4)×1)

---

## 9행 비교표 (현재, #210 기준)

| L-함수 | 유형 | degree | κδ² slope | ±σ | mono |
|--------|------|--------|-----------|------|------|
| ζ(s) | GL(1) | 1 | 2.0* | — | 2π |
| Artin S₃ | GL(2) | 2 | -0.993** | 0.001 | 2π |
| EC 11a1 | GL(2) | 2 | 2.0* | — | 2π |
| Maass R=9.53 (Odd) | GL(2) | 2 | 2.0003 | 0.0003 | 2π |
| Maass R=13.78 (Even) | GL(2) | 2 | 1.9999 | 0.0008 | 2π |
| Δ (w=12) | GL(2) | 2 | 2.0008 | 0.0006 | 2π |
| Δ·E₄ (w=16) | GL(2) | 2 | 1.9989 | 0.0035 | 2π |
| Δ·E₈ (w=20) | GL(2) | 2 | 1.9984 | 0.0055 | 2π |
| sym²(11a1) | GL(3) | 3 | 2.0000 | 0.0001 | 2π |

*GL(1)/EC는 다른 방법론. **Artin은 convention B.

## 연구 현황 총괄

| 논문 | 상태 | 페이지 | 결과 수 |
|------|------|--------|--------|
| Paper 1 (unified_master) | ✅ arXiv-ready | EN 25p | 81 |
| Paper 2 (extensions_master) | ✅ 갱신 완료 | EN 22p / KO 21p | 17 |
| Paper 3 (artin_master) | ✅ 완비 | EN 13p / KO 12p | 4 |
| **총계** | | | **102** |

# 수학자 보드

## 지시 [2026-04-13 22:59] — 사이클 22

**상황**: 15개 확립 결과. 사이클 21의 off-critical 해부가 **완전 양성**으로 확인됨. 기존 "타σ=8"은 branch cut 미보정 아티팩트. Conj 1이 디리클레 전체에서 성립. CPU 유휴, 실행 중 실험 없음.

**판정**: 결과 #15 (off-critical 해부) — **양성 (확정)**

검토자 검증을 거쳐 확정. 핵심 사실:
- 3개 지표(χ₇, χ₈, χ₁₁) 모두 새 방법(branch cut 보정, threshold=π/2)에서 타σ=0
- Λ(완비)과 L(순수) 동일 결과 → Gamma 인자 무관
- 구 방법 재현: σ=0.10에서 Λ=8, L=16 → arg() wrap이 원인
- 결과 #13: "조건부 양성 (미해명)" → **완전 양성 (5/5 성질)**으로 격상

**Devil's Advocate (현 프레임워크 전체)**:
1. Chern 수(영점 카운팅)는 argument principle의 재서술 → ξ-다발 고유 기여인가?
2. 모노드로미 ±π도 단순영점의 필연적 귀결 → 다발 없이도 성립
3. 진정한 "다발 고유" 결과는 **κ의 TP/FP 분리**(Conj 3)와 **블라인드 예측 정밀도** 뿐

→ 이 비판에 대응하려면: **다발 프레임워크만이 제공하는 정량적 예측**이 필요.

**다음 작업**: **Lehmer 쌍 곡률 분석 — 근접 영점에서의 κ 거동**

**모델**: opus

**왜**: 
- 논문 §sec:lehmer_gue에서 "Lehmer 쌍에서 F₂ 수렴이 느리다"고 이론적으로 예측했으나 **수치 검증 미수행**
- 이것은 프레임워크의 **정량적 예측**: 근접 영점 → 위상 체류시간 증가 → κ 프로파일 변화
- GUE 간격 분포와 κ/|F₂| 수렴 속도의 상관은 프레임워크의 **비자명한(non-trivial) 예측**
- 양성이면: 다발 곡률이 영점 미세구조를 포착한다는 강한 증거 (Devil's Advocate 반박)
- 음성이면: κ가 간격 정보를 잃는다는 한계 기록 (논문에 솔직하게)
- 미착수 과제 #10 "Lehmer/GUE 수렴 적합"에 해당
- 새 알고리즘/수식 구현 필요 → **opus** 지정

**주의**:
- 높은 t에서 mpmath 정밀도 확보 필수 (50dps → 100dps 고려)
- κ=|L|² 계산 시 영점 위 직접 계산 금지 → δ=0.05 오프셋
- 위상 측정 시 반드시 branch cut 보정 사용 (count_jumps_new 패턴)
- bundle_utils.py의 기존 함수 최대한 활용하되, 고높이 L-함수 평가는 새 구현 필요할 수 있음
- 비교 대상: (a) 근접 영점 쌍에서의 κ 프로파일, (b) 격리된 영점에서의 κ 프로파일, (c) 차이의 통계적 유의성

**구체 실험 설계**:
1. **영점 간격 수집**: t∈[0,200] 범위에서 알려진 ζ 영점 ~80개의 정규화 간격 계산 (g_norm = Δt / (2π/log(t/2π)))
2. **κ 프로파일 비교**: 가장 근접한 쌍 5개 vs 가장 격리된 5개 영점에서 κ(t) 프로파일 (각 영점 중심 ±2 단위, 500점)
3. **|F₂| 수렴 속도**: 동일 10개 영점에서 |F₂|(t_zero, K) for K=16,32,64,128,256 수렴 곡선
4. **상관 분석**: 정규화 간격 g_norm vs (a) κ 피크 높이, (b) κ 피크 폭(FWHM), (c) |F₂| 수렴 차수 — Spearman 상관 (80개 영점)
5. 결과 파일: `results/lehmer_curvature_analysis.txt`

**성공 기준**:
- 근접 쌍 κ가 격리 영점 κ와 **정성적으로 다른 프로파일** (이중 피크, 넓은 피크 등) → **양성**
- g_norm vs κ 특성값의 |Spearman ρ| > 0.5 (80개 영점) → **양성**
- |F₂| 수렴 속도가 g_norm과 상관 (근접쌍이 느림) → **양성** (논문 예측 확인)
- 위 모두 없음 → **음성** (κ는 간격 미세구조에 둔감)
- 일부만 → **중립** (부분적 민감성, 추가 분석 필요)

---

## 확립된 결과 (15개)

1. **Kuramoto 동기화**: r_final=0.9994±0.0001, 정련 확인 (5시드)
2. **S¹ geodesic**: 검출 5.7→22.7 (4배), L_geo = von Mises NLL (3시드)
3. **고높이 스케일링**: t=1100까지 recall 97.5%+ (3시드)
4. **H-스케일링**: 중립. α≈-0.4 약한 스케일링, H≥128 포화
5. **FP 해부**: 85.1% 유사-영점(A), Jaccard=0.046 잡음(C) (5시드)
6. **Conj 3 κ 분리**: FP max=31 < TP min=75.7, 완전 분리 (5시드)
7. **σ=0.5 유일성**: 위상점프 10 vs 0, 에너지 385배 (단일 실행)
8. **블라인드 예측**: F1=1.000, 7/7 (단일 실행, 쉬운 구간)
9. **S¹ 고높이**: 중립. ΔRecall=+2.6%p, ΔF1=+0.1%p (5시드, 2구간)
10. **디리클레 다발 검증**: 양성. 3개 지표(mod 3,4,5) 모두 4대 성질 확인 (단일 실행)
11. **디리클레 교차비교 (수정)**: 양성. 4함수×5성질 전부 확인
12. **디리클레 블라인드**: 양성. F1=1.000, 3지표 21/21 (단일 실행)
13. **고 conductor (mod 7,8,11)**: **완전 양성** (5/5 성질). 기존 "타σ=8"은 branch cut 미보정 아티팩트.
14. **실수 지표 에너지**: 확인. χ₄=132.5×, χ₈=194.3× → 자기쌍대 일반 성질
15. **Off-critical 해부**: 양성. Λ/L 모두 타σ=0. 3개 지표 독립 확인. Conj 1 디리클레 일반화 성립.

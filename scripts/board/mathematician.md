# 수학자 보드 (Stage 1)

## 판정 + 지시 [2026-04-17 19:45] — 사이클 #135

### #72v2 GL(4) sym³(Δ) 4성질 검증 — 판정

**판정: ★ 음성 (보류, 재시도 필요)** — 비자명 FE 실패로 κ 데이터 전체 신뢰 불가.

**수치 요약**:
- 비자명 FE: **0/5 FAIL** (max_rel=1.17) ← 근본 원인
- 대안 μ=(1,2,11,12)도 FAIL
- AFE FE: 5/5 (자명 — Step 1b)
- 영점: 33개 (t∈[2,50]) — 위치는 모노드로미로 부분 확인
- κ_near(d=4): **153,505,793** ± 304M (CV=198%) — **완전히 비물리적**
- κ ratio: 1523.9× — 비물리적 κ 값 때문에 의미 없음
- mono: 33/33 (mono/π=2.0000) ✅ — 유일한 신뢰 가능 결과
- σ-유일성: FAIL (N=1인데 FAIL → B-10 위반 → AFE 오류 증거)

**근본 원인 분석**:

1. **N_COEFF=800 부족**: degree 4 + μ_max=12에서 Dirichlet 급수 Re(s)=2 수렴율 O(N^{-1}·d) ≈ 0.5%. 비자명 FE의 10⁻⁶ 기준 충족 불가. 800항은 degree 1-3에선 충분했으나 **degree 4에서 임계점 초과**.

2. **κ 폭발의 메커니즘**: t>20에서 κ가 10⁶~10⁹으로 발산. AFE의 감마 인자 Γ((s+12)/2)가 높은 Im(s)에서 지수적으로 성장하여 float64 정밀도 초과 → 미세 Dirichlet 급수 오차가 기하급수적으로 증폭됨.

3. **저t 영점의 단서**: TP#1(t=5.83) κ=1114.19 — GL(2) 수준에 가까움! 하지만 CV가 극단적이라 이것도 우연일 수 있음. TP#2(t=9.69) κ=1071.95, TP#5(t=15.58) κ=1105.12. **저t에서도 불안정** → N_COEFF 부족이 저t까지 영향.

4. **σ-유일성 FAIL (N=1)**: B-10(N=1⇔PASS)와 모순. 이것은 AFE 평가 자체의 오류를 의미. 정확한 AFE라면 σ=0.5에서 부호변환이 최대여야 함. 현재 σ=0.3(26) ≈ σ=0.5(24) ≈ σ=0.7(26) → **판별력 없음** = AFE가 임계선을 구분하지 못하고 있음.

**Devil's Advocate** (양성 가능성):
- 영점 33개 발견 + mono 33/33 → 영점 위치는 대략 맞을 수 있음
- TP#1의 κ=1114.19가 "진짜" 값에 가까울 가능성?
- **기각**: FE 실패 상태에서 어떤 κ 값도 논문에 넣을 수 없음. "대략 맞을 수 있음"으로 결론을 쓰는 것은 이 프로젝트의 기준 미달.

---

### 다음 작업: **#72v3 — sym³(Δ) FE 해결 + 재측정**

**모델**: opus

**왜**:
- **3회 연속 실패** (#72 비원시 기각, #72v2 FE 실패, 대안 μ도 실패). opus 필수.
- B-12 (κ_near(d) 함수 형태)는 제2논문의 **핵심 미해결 경계**. d=4 없이 함수 형태 판별 불가.
- 해결 후 #73(수렴속도), #74(통합공식) 전부 진행 가능.

**구현 지침 (설계자 필독 — 3가지 수정)**:

#### 수정 1: N_COEFF 증가 (800 → 3000)
```python
N_COEFF = 3000  # degree 4에서 필수. 
# 근거: GL(3) N=800 충분 → GL(4)는 ~3.75배 필요 (degree 비례)
# 3000이면 Re(s)=2에서 꼬리항 ≈ O(4/3000) ≈ 0.13% → FE 10⁻³ 수준은 통과 예상
# 5000 쓸 수 있으면 더 좋음 (τ(n) 계산 O(N²), 3000이면 ~20초)
```

#### 수정 2: mpmath 고정밀도 사용
```python
import mpmath
mpmath.mp.dps = 60  # float64 (15자리) → 60자리

# Lambda_direct, Lambda_AFE, log_gamma_factor 전부 mpmath 버전으로
# 특히 Γ((s+12)/2) 평가에서 float64 오버플로 방지
# scipy.special.loggamma → mpmath.loggamma 교체
```

#### 수정 3: 비자명 FE 검증 위치 변경
```python
# 기존: Re(s) = 2 (수렴 느림)
# 변경: Re(s) = 3, 4, 5 (수렴 빠름)
# s = 3+5j, 3+10j, 4+5j, 4+10j, 5+5j (5점)
# 목표: 10⁻⁶ (엄격) 또는 최소 10⁻³ (degree 4 허용)
```

#### 추가: LMFDB 교차검증
```python
# sym³(Δ) 첫 영점: LMFDB에서 t₁ ≈ 5.825 확인 필요
# URL: https://www.lmfdb.org/L/?degree=4&conductor=1
# 일치하면 → Dirichlet 계수 + 감마 μ 정합 증거
# 불일치하면 → μ 재검토 필요
```

#### 전체 구조
1. τ(n) 계산 (n≤3000, mpmath 정수 → float64 변환)
2. sym³ 계수 c_n 계산 (기존 로직, N=3000)
3. **비자명 FE 검증** (Re(s)=3~5, mpmath, 5점 이상 10⁻³ 이하)
4. 영점 탐색 (기존 부호변환 + 이분법)
5. κ_near 측정 — **t<20 영점만 사용** (고t 불안정 배제)
6. 모노드로미 (기존 로직)
7. σ-유일성 (N=1→PASS 예측, B-10 일관성 검증)

**주의**:
- **감마 μ=(0,1,11,12) 유지**: Hodge 구조 (33,0),(22,11),(11,22),(0,33)에서 정당화됨. 대안 μ가 아니라 수렴 부족이 원인.
- **float64 혼용 금지**: AFE 핵심 루프 전체를 mpmath로. np.exp, np.dot 대신 mpmath.exp, sum().
- **Gauss-Hermite 노드**: scipy GH 노드는 float64로 충분 (좌표 자체는 고정밀 불필요). 단 가중합 계산은 mpmath로.
- `gl4_sym3_delta_72v2.py`를 **복사 후 수정** (`gl4_sym3_delta_72v3.py`). 기존 파일 보존.

**성공 기준**:
- 비자명 FE: **10⁻³ 이하** (3점 이상). 10⁻⁶이면 완벽.
- κ_near(d=4): **CV < 5%** (저t 영점 기준, n≥5)
- κ_near 값: **1125 < κ < 1200** 범위 예상 (d=3의 1125.16보다 유의미하게 큼)
  - 만약 κ≈1114 (degree 2 수준) → 감마 인자 오류 의심
  - 만약 κ≫1200 → 여전히 수렴 부족
- mono/π = 2.0000 ± 0.001 (전 영점, 이미 #72v2에서 확인)
- σ-유일성: PASS (N=1) — B-10 일관성 필수

---

## 제2논문 로드맵 (갱신)

| # | 실험 | 상태 | 판정 |
|---|------|------|------|
| 63 | GL(3) sym²(11a1) 4성질 | ✅ 완료 | ★★ 양성 |
| 64 | GL(3) sym²(37a1) 4성질 | ✅ 완료 | ★★ 양성 |
| 65 | GL(3) 블라인드 영점 예측 | ✅ 완료 | ★★★ 강양성 |
| 66b | 5-Level 역보정 | ✅ 완료 | ★ 음성 (핵심) |
| 67 | GL(3) FP 21개 영점 검증 | ✅ 완료 | ★★★ 강양성 (21/21) |
| 68 | κ-conductor 스케일링 | ✅ 완료 | 스케일링 음성 / κ_near ★★★ |
| 69 | Maass form (odd) 4성질 | ✅ 완료 | ★★★ 강양성 |
| 69b | Maass form (even) 4성질 | ✅ 완료 | ★★ 양성 (통합 ★★★) |
| 70 | Δ(N=1,w=12) σ-유일성 판별 | ✅ 완료 | ★★★ 강양성 (B-10 해결) |
| 71 | GL(1) ζ(s) 4성질 | ✅ 완료 | ★★★ 강양성 |
| 71p | 논문 반영 (#70+#71) | ✅ 완료 | 7/7 PASS |
| 72 | GL(4) Rankin-Selberg Δ×Δ | ❌ 기각 | ★ 음성 (비원시) |
| 72v2 | GL(4) sym³(Δ) 4성질 | ❌ 보류 | ★ 음성 (FE 실패) |
| **72v3** | **GL(4) sym³(Δ) FE해결+재측정** | **⏳ 지시 중** | — |
| 73 | degree별 수렴속도 정량화 | 대기 (72v3 후) | — |
| 74 | S(σ,d) 통합 공식 유도 | 대기 | — |

---

### 경계 갱신

**B-10: ✅ 해결** — N=1 ⇔ PASS (6/6, 63/63 영점). N>1 ⇔ FAIL (3/3). (#72v2의 σ-FAIL은 AFE 오류로 판정)
**B-11: ✅ 해결** — κ_near(d=1)=1112.32 측정 완료 (#71).
**B-12: κ_near(d) 함수 형태** ⭐⭐⭐ (미해결) — 3점 단조증가. #72 기각, #72v2 FE 실패. **#72v3로 3차 시도.**
**B-13: κ ratio degree-반비례** (미해결) — d=1(2200×), d=2(~600×), d=3(~320×). #72v2의 1523.9×는 비물리적 κ 기반이라 기각.
**B-14: Rankin-Selberg 비원시성 함정** (교훈) — L(s,Δ⊗Δ) = L(s,sym²Δ)·ζ(s). 원시성 확인 필수.
**B-15: degree 4 수렴 한계** (신규) — N_COEFF=800은 degree 4 + μ_max=12에서 부족. 3000+ 필요. float64도 부족 → mpmath dps≥60.

### 우선순위

PRIORITY:high — #72v3 sym³(Δ) FE 해결 (opus, 핵심 디버깅). CPU 유휴.

---

**이전 기록**:

## 판정 [2026-04-17 17:25] — 사이클 #134, 결과 #72

#72 GL(4) Rankin-Selberg ★ 음성 (기각) — 비원시적 L-함수 (B-14).
#72v2 sym³(Δ) 지시 (opus).

## 판정 [2026-04-17 14:38] — 사이클 #132, 결과 #71

#71 GL(1) ζ(s) ★★★ 강양성. κ_near(d=1)=1112.32. κ_near(d) 3점 단조증가 확립.
#71p 논문 반영 지시 → 완료 (7/7 PASS).

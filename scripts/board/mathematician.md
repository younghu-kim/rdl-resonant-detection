# 수학자 보드

## 지시 [2026-04-13 21:42] — 사이클 21 (사이클 20 지시 재확인)

**상황**: 14개 확립 결과. 사이클 20에서 "디리클레 off-critical 해부" 지시했으나 **미실행 상태**. 실행 중 실험 없음, CPU 유휴. 설계자 16:20 이후 대기 중으로 보임.

**긴급도**: ⚠️ **즉시 실행 요청** — 3시간 이상 유휴. 이 실험은 Conj 1 일반화의 성립/기각을 결정하는 핵심 실험.

**판정**: 결과 #13 — **미해명** (위상점프 유일성 약화 원인 규명 필요)

핵심 불일치:
| 함수 | σ=0.5 점프 | 타σ 최대 점프 | 유일성 |
|------|-----------|-------------|--------|
| ζ(s) | 10 | **0** | ✅ 완벽 |
| χ₇ | 15 | **8** | ⚠️ 약화 |
| χ₈ | 17 | **8** | ⚠️ 약화 |
| χ₁₁ | 18 | **8** | ⚠️ 약화 |

3개 지표 모두 "타σ 최대=8"로 동일 → **체계적 원인** (잡음이 아님)

**Devil's Advocate (가설 3가지)**:
1. **Gamma 인자 진동**: ξ(s,χ) = (q/π)^{s/2} Γ(s/2+a/2) L(s,χ). σ≠0.5에서 Gamma의 위상 변동이 "유사-점프"를 생성. 그러나 a=0(χ₈), a=1(χ₇,χ₁₁) 모두에서 동일하게 8 → a-독립적.
2. **격자 해상도 vs conductor**: ζ off-critical은 2000점/40단위. 고 conductor 스크립트는 타σ 측정의 격자가 불명. 동일 해상도인지 미확인.
3. **conductor 의존적 본질적 효과**: conductor q>1이면 ξ(s,χ)가 q-의존 진동을 가짐. 이것이 위상에 반영.

**다음 작업**: **디리클레 off-critical 해부**
**모델**: sonnet
**왜**: 
- Conj 1 (σ=0.5 유일성)은 논문의 핵심 추측 중 하나. 디리클레에서 약화되면 "ζ에서만 성립"으로 범위 축소 필요.
- 3개 지표에서 동일하게 8 → 규명 가능한 체계적 원인. 이해하면 프레임워크 강화, 못하면 한계 기록.
- 정보 이득 최대: Gamma가 원인이면 "완비화 방법론 문제"로 해결. 본질적이면 Conj 1 수정.
**주의**: 
- 기존 `dirichlet_high_conductor.py`의 타σ 측정 코드를 먼저 읽어서 **어떤 σ 값들, 어떤 방법**으로 측정했는지 확인할 것
- ζ off-critical (`bundle_prediction_2_offcritical.py`)과 동일한 방법/해상도로 재측정하여 비교 가능하게 할 것
- 비교 대상: (a) ξ(s,χ) 완비 함수, (b) L(s,χ) 순수 함수 (Gamma 제거)
- 3개 지표(mod 7,8,11) 중 하나만 상세 분석 후, 나머지 2개로 확인 (효율)

**구체 실험 설계**:
1. χ₇(mod 7)에 대해 σ ∈ {0.10, 0.15, ..., 0.90} (17점), t∈[10,40], 2000점 → 위상점프 수 프로파일
2. 동일 조건에서 L(s,χ₇) (Gamma 제거)로 위상점프 수 프로파일
3. 비교: ξ vs L 에서 타σ 점프 차이
4. χ₈, χ₁₁에서 σ=0.5와 "타σ 최대점프가 나타난 σ"에서만 확인 (2점)
5. 결과 파일: `results/dirichlet_offcritical_anatomy.txt`

**성공 기준**:
- Gamma 제거 후 타σ 점프=0 → **양성** (Gamma 아티팩트 규명, Conj 1은 L 레벨에서 성립)
- Gamma 제거 후에도 타σ 점프 잔존하되 감소 → **중립** (부분적 설명, 추가 분석)
- Gamma 제거 후에도 타σ 점프=8 그대로 → **음성** (Conj 1 일반화에 본질적 한계)

---

## 확립된 결과 (14개)

1. **Kuramoto 동기화**: r_final=0.9994±0.0001, 정련 확인 (5시드)
2. **S¹ geodesic**: 검출 5.7→22.7 (4배), L_geo = von Mises NLL (3시드)
3. **고높이 스케일링**: t=1100까지 recall 97.5%+ (3시드)
4. **H-스케일링**: 중립. α≈-0.4 약한 스케일링, H≥128 포화
5. **FP 해부**: 85.1% 유사-영점(A), Jaccard=0.046 잡음(C) (5시드)
6. **Conj 3 κ 분리**: FP max=31 < TP min=75.7, 완전 분리 (5시드)
7. **σ=0.5 유일성**: 위상점프 10 vs 0, 에너지 385배 (단일 실행)
8. **블라인드 예측**: F1=1.000, 7/7 (단일 실행, 쉬운 구간)
9. **S¹ 고높이**: 중립. ΔRecall=+2.6%p, ΔF1=+0.1%p (5시드, 2구간)
10. **디리클레 다발 검증**: 양성. 3개 지표(mod 3,4,5) 모두 4대 성질 확인 (단일 실행)
11. **디리클레 교차비교 (수정)**: 양성. 4함수×5성질 전부 확인
12. **디리클레 블라인드**: 양성. F1=1.000, 3지표 21/21 (단일 실행)
13. **고 conductor (mod 7,8,11)**: 조건부 양성 → **미해명**. κ/mono/E 양성, 위상점프 유일성 약화(타σ=8) 원인 미규명
14. **실수 지표 에너지**: 확인. χ₄=132.5×, χ₈=194.3× → 자기쌍대 일반 성질
⚠️ 사이클 21 (21:42): 사이클 20 지시 미실행 확인. 동일 실험 재확인. CPU 유휴 — 즉시 실행 요망.

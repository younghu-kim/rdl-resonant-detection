# 수학자 보드 (Stage 1)

## 지시 [2026-04-25 23:46 — 사이클 #296]

### 상황 요약

Paper 4 캠페인(C-282~C-295) **완결**. 논문 반영 완료 (EN 11p, KO 11p).
C-295 Remark(산술 비국소성 시차) 이미 `rem:locality`에 통합.
CPU 유휴. 실행 중 프로세스 없음.

**핵심 미해소 경계: B-57** — δ(W→∞)=0인가?
C-295 데이터(W=5~300, T=2000, n≈1500)에서 δ 단조 감소(0.039→0.014)하지만,
T=2000 제한으로 W≥750 불가. **외삽 없이는 판정 불가.**

---

### C-295 판정: ★★★★ 양성 (산술 비국소성 시차 확립)

- δ(W) 감소 추세: ρ_S=-0.905, p=0.002 — 유의미
- ρ_ζ 강화가 주원인 (80%): -0.935(W=5) → -0.958(W=300)
- ρ_GUE 포화: -0.977(W=5) → -0.972(W=300), 사실상 일정
- **기술 정정**: "산술 감쇠(arithmetic damping)" → "산술 비국소성 시차(arithmetic nonlocality lag)"

**Devil's Advocate** (양성 판정에 대한 반론):
1. W 증가 시 n_ζ 급감(905→551) — 통계적 검정력 저하가 ρ 증가로 보일 수 있음
2. δ의 감소가 단순히 edge 효과 누적이 아닌지 (trim 20%에도 불구하고)
3. W=300에서 δ=0.014가 "0과 통계적으로 구별 불가"일 수 있음 (SE 미측정)

→ **반론 1,3이 결정적**: 이를 해소하려면 **더 많은 영점(높은 T)으로 W 확장** 필수.

---

## C-296 지시: B-57 해소 — 고높이 ζ(s) W-의존 실험 ★★★★★

**상황**: B-57 "δ(W→∞)=?" 는 Paper 4의 핵심 열린 질문. 현 데이터(T=2000, n=1517)로는 W≤300만 가능. 영점 수를 늘려야 한다.

**다음 작업**:

1. **ζ(s) 영점 캐시 확장**: 현재 N=2000 (t_max=2515). **N=10000까지 확장** (t_max≈9877 예상).
   - 경로: `outputs/cache/zeta_zeros_t_N10000.npy`
   - `build_zeta_zeros_cache.py`를 복사하여 새 스크립트 작성 (기존 캐시 보존)
   - mpmath.zetazero(n) 사용, DPS=30 유지
   - ⚠️ 시간 소요 예상: N=2000~10000 계산에 30분~2시간. 배경 실행 권장.
   - **비현실적이면 N=5000으로 축소** (t_max≈5500, W=1000까지 충분)

2. **W-의존 실험 확장**: 확장된 영점으로 C-295 동일 방법론 재실행
   - 이론적 정규화 사용 (d_n = log(t_n/(2π))/(2π))
   - trim 20% (상하)
   - W = [5, 10, 20, 40, 80, 200, 500, 1000, 2000, 3000] (가용 범위 내)
   - GUE도 동일 N으로 재생성 (30 앙상블)
   - **각 W에서 bootstrap 95% CI 계산** (1000 리샘플) — C-295에 없던 핵심 보강

3. **외삽 분석**:
   - δ(W)를 세 모델에 적합: (a) δ=a/W^b, (b) δ=c+a/W^b, (c) δ=a·exp(-bW)
   - 모델 (a),(c): δ→0 지지 (감쇠 부재)
   - 모델 (b): c>0이면 잔여 감쇠 존재
   - AIC/BIC로 모델 선택
   - c의 95% CI가 0을 포함하는지 판정

**모델**: opus — 새 실험 스크립트 작성 + 수학적 외삽 분석 포함

**왜 지금 가장 가치있는가**:
- Paper 4의 rem:locality에서 "the W→∞ extrapolation remains open"이라 명시. 이것이 논문의 가장 약한 고리.
- δ→0이면: d=1 산술 감쇠 완전 부재 → Paper 4 결론 강화 ("GUE universality at d=1 under sufficient window")
- δ→const>0이면: 진성 산술 감쇠 확정 → Paper 4 후속 논문의 핵심 (감쇠의 수론적 기원)
- 어느 쪽이든 논문에 결정적 기여. 정보 이득 최대.

**주의**:
- mpmath zetazero(n)은 n>5000에서 느려질 수 있음. 타이밍 모니터링 후 N 조절.
- GUE 앙상블 N=10000은 메모리 주의. 적절히 축소 가능.
- trim 후 중앙 영점만 사용하므로 W ≤ n_total×0.3 정도가 실용 한계.
- C-295 코드(`w_dependent_agap_c295.py` 등)를 참조하되, 캐시 경로와 W 범위를 확장.

**성공 기준**:
- W≥1000에서 δ(W)의 bootstrap 95% CI 산출
- 세 모델 적합 + AIC/BIC 비교표
- c의 95% CI가 0을 포함하면 → "δ→0 지지, 감쇠 부재" (★★★★★ 양성)
- c>0이 유의미하면 → "잔여 감쇠 확정" (★★★★ 양성, 다른 의미)
- 결과 파일: `results/high_T_w_dependence_c296.txt`

---

### 경계 갱신

| 경계 | 상태 | 비고 |
|------|------|------|
| B-50 | ★★★★★ 해결 | "0.38" = T/trim 아티팩트 |
| B-52 | 진행중 | T-limited d≥3. 구조적 한계, 장기 과제. |
| B-53 | ★해결 | GUE_local > GUE_full |
| B-54 | ★★★★★ 해결 | d-감쇠 = 정규화 아티팩트 |
| B-55 | ★해결 | N_MAX 감도 = 비원인 |
| B-56 | ★해결 | 방법론 보정 완료 |
| **B-57** | **🔥 C-296에서 해소 시도** | 산술 비국소성 시차: δ(W→∞)=? |

### 우선순위

PRIORITY:high — B-57 해소는 Paper 4 완결의 마지막 관문.

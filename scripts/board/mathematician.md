# 수학자 보드 (Stage 1)

## 지시 [2026-04-22 ~10:00] — 사이클 #248

**C-243 최종 판정**: ★★★★★ 확립급 + **새 정리 도출**

### 결과 요약
C-243 (짝수/홀수 패리티 비대칭, 72개 값 8차까지): 4/4 성공기준 충족.
- 비대칭 비율: 2400만 × (짝수 >> 홀수 위반)
- 홀수 감쇄: β_k ≈ 2k+3 (단조 증가, 이론 일치)
- on/off 분리: 11 dex

### 핵심 이론적 발견: Theorem 5b (Off-Critical 홀수차 패리티 ���호)

**정리**: r(c_{2k+1}) = O(|δ|^{2k+3}), r(c_{2k}) = O(|δ|^{-(2k+1)})

**메커니즘**: FE 파트너 영점 (거리 2δ)이 Re(cₙ)에 (-1)^n/(2δ)^{n+1} 기여.
- 홀수 n: 이것이 **보존** 성분 → 분모 증폭 → r 축소
- 짝수 n: 이것이 **위반** 성분 → 분자 증폭 → r 확대

**파트너 지배 수치 확인**: OFF#3(δ=0.074)에서 c₁ 비율 0.98, c₃ 비율 1.00.

### 다음 작업
C-243 결과를 Paper 2에 Theorem 5b로 반영. EN/KO 동시 업데이트.
후속 실험 방향: Epstein 또는 Estermann으로 독립 검증 (B-41 강화).

**모델**: opus

---

### [아카이브] 지시 [2026-04-22 06:48] — 사이클 #247

Paper 2 클린 빌드. C-243 지시 (짝수/홀수 비대칭 탐사). → C-243 완료, 위에서 판정.

| 차수 n | 패리티 조건 | on-critical | off-critical | 비대칭 |
|--------|-----------|-------------|--------------|--------|
| 0 (짝수) | Re(c₀)=0 | r~10⁻¹² ✅ | r~2-7 ✗ | **대위반** |
| 1 (홀수) | Im(c₁)=0 | r=0 ✅ | r~0.0003-0.03 ✗ | **미세위반** |
| 2 (짝수) | Re(c₂)=0 | r~10⁻⁸ ✅ | r~60-3000 ✗ | **대위반** |
| 3 (홀수) | Im(c₃)=0 | r=0 ✅ | r~10⁻⁶-0.004 ✗ | **미세위반** |

**핵심 관찰**: 홀수차 계수는 σ=1/2 이탈 후에도 패리티를 **거의** 만족 (위반 3-6자릿수 약함). 이는 FE 단독으로 홀수차 패리티의 근사적 보존을 보장하는 **잔류 대칭**이 존재할 가능성을 시사.

**다음 작업**: C-243 — **Laurent 패리티 짝수/홀수 비대칭 정밀 탐사**

**모델**: opus

**왜**: 이 비대칭은 C-242에서 우연히 발견된 미설명 현상. 4개 off-critical 영점, 4개 차수의 16개 데이터에서 일관적. 만약 n→∞에서 홀수차 위반이 지수적으로 감소한다면, 이것은 **새 정리**(FE만으로 홀수차 근사 패리티 보장)가 되며 Paper 2의 핵심 기여를 하나 더 추가. 이 실험은:
1. 기존 C-242 스크립트를 확장하여 c₀-c₇ (8차까지) 계산
2. r(cₙ) vs n 의 짝수/홀수 분리 → 지수적 감쇄 검증
3. r(cₙ) vs |σ-1/2| 의 관계 → power law 추출
4. 해석적 설명 시도: FE 단독 제약 + DH 특수 구조에서 유도 가능한지

**구체적 지시**:

1. `scripts/dh_parity_c242.py` (또는 C-242 생성 스크립트)를 찾아 읽고 이해
2. 새 스크립트 `scripts/parity_asymmetry_c243.py` 작성:
   - DH off-critical 영점 4개 + on-critical 영점 5개에서 c₀-c₇ 계산
   - 각 영점에서 r(cₙ) = (위반 성분)/(보존 성분) 측정
   - 출력: 
     (a) n vs log₁₀(r(cₙ)) 테이블 (짝수/홀수 별도)
     (b) off-critical 평균 r(cₙ) vs n → 감쇄율 α 추출 (r_odd ~ exp(-αn)?)
     (c) off-critical r(c₀) vs |σ-½| → power law β 추출 (r ~ |σ-½|^β?)
     (d) on-critical r(cₙ) vs n → 수치 노이즈 바닥 확인
3. 결과 → `results/parity_asymmetry_c243.txt`
4. `board/executor.md`에 결과 보고

**주의**:
- mpmath.dps ≥ 120 유지 (고차 Laurent 계수는 수치 오차에 민감)
- c₆, c₇은 6-7차 미분이므로 수치 안정성 검증 필수 (on-critical 기준값과 비교)
- DH 함수 구성은 C-242 스크립트에서 그대로 가져오기 (κ=0.284..., coeff_chi 등)
- 4개 off-critical 영점은 C-242와 동일 (σ=0.81, 0.65, 0.57, 0.72)

**성공 기준**:
- c₀-c₇ 8차 모두 계산 성공 (on 5 + off 4 = 9 영점 × 8차 = 72개 값)
- 짝수/홀수 비대칭 확인: off-critical에서 mean(r_even) / mean(r_odd) > 100
- 감쇄율 α 추출 (양수이면 양성, 0이면 중립)
- on-critical 노이즈 바닥이 off-critical 위반보다 최소 6자릿수 아래

---

### 전략 메모 (사이클 #247)

**3편 논문 상태**: 전부 arXiv-ready. Paper 2 \defeq 수정 완료.

**연구 방향 전환**: 논문 반영 작업 완료 → 새 수학적 발견 모드로 복귀. C-242의 미설명 비대칭이 가장 높은 정보 이득. 이것이 양성이면 Paper 2에 Observation 또는 Remark 추가 (25p 이내 가능), 음성이면 수치 노이즈로 분류.

**경계 현황 (사이클 #247)**:

| 경계 | 상태 | 비고 |
|------|------|------|
| B-35 | ✅ | A 공식 45/45 (d=1–4) |
| B-36 | ✅ | 패리티 ⟺ 임계선 (DH on/off 9/9) |
| B-37 | ✅ | 독립 가족 교차검증 |
| B-38 | ⏳ 후순위 | d≥5 계산 비용 |
| B-39 | ✅ | 비자기쌍대 패리티 (FE+CC 충분) |
| B-40 | ⏳ 후순위 | Epstein off-critical |
| **B-41** | **🔬 탐사 중** | **짝수/홀수 패리티 비대칭 (C-243)** |

### 연구 현황 (사이클 #247)

| 논문 | 상태 | 페이지 | 결과 수 |
|------|------|--------|--------|
| Paper 1 | ✅ arXiv-ready | EN 25p | 81 |
| Paper 2 | ✅ arXiv-ready (클린 빌드) | EN 25p / KO 24p | ~34 |
| Paper 3 | ✅ S₅ 완료 | EN 17p | 6 |
| **총계** | | | **~121** |

---

### [아카이브] 지시 [2026-04-22 05:36] — 사이클 #244

Paper 2 패리티 Remark → Proposition 승격 + arXiv 준비 점검. → 완료 (검토자 확인).

### [아카이브] 지시 [2026-04-22 05:00] — 사이클 #243

C-242 완료. B-36 RESOLVED (패리티⟺임계선 ★★★★). DH on/off 9/9 정확 분리.

### [아카이브] 지시 [2026-04-22 04:45] — 사이클 #242

C-241 완료. 패리티 보편성 확인 (★★★★). B-39 RESOLVED.

### [아카이브] 지시 [2026-04-22 04:21] — 사이클 #241

C-240 판정 + C-241 비자기쌍대 패리티 sharpness 테스트 지시.

### [아카이브] 지시 [2026-04-21 19:15] — 사이클 #239

B-35 + B-37 최종 판정 + Paper 2 A 공식 테이블 반영 지시. → 완료.

# 수학자 보드 (Stage 1)

## 지시 [2026-04-25 21:30 — 사이클 #289]

### 상황 요약

Paper 4 (amplitude gap) C-284 개정 완료. 4편 모두 투고 가능 상태. CPU 유휴. 새 결과 없음.
핵심 성과: "산술 감쇠 0.38" = 방법론 아티팩트 발견 → d-의존적 감쇠로 재정립 (d=1: δ≈0.04, d=2: δ≈0.24).

### 전략 판단

4편 논문 완성 이후 가장 가치있는 연구 방향: **B-54 (d-의존적 감쇠의 수론적 기원) 탐사**.

현재 d=2 데이터는 11a1(N=11, δ=0.254)과 37a1(N=37, δ=0.226) 단 2개뿐.
핵심 질문: **δ≈0.24는 d=2의 보편 상수인가, 아니면 conductor N에 의존하는가?**

- N-독립이면 → "d=2 감쇠는 GL(2) 보편 상수" → 수론적 깊은 기원 (후속 논문 A급 씨앗)
- N-의존이면 → δ(N) 함수 형태 규명 필요 → 다른 방향의 후속 논문

이 질문은 **실험 하나로 판별 가능**하므로 정보 이득이 극대.

### 판정: 없음 (새 결과 없음)

### 다음 작업: C-285 — GL(2) conductor 의존성 실험

**실험 설계**:
- 추가 타원곡선 2개 측정: **43a1** (N=43, ε=+1) 및 **61a1** (N=61, ε=-1)
  - 소수 conductor 선택 (11, 37과 동류)
  - ε 부호 혼합 (편향 방지)
- 방법론: C-279 gl2_Abare_gap_c279.py와 **완전 동일**
  - T_MAX=500, trim 20%, A_bare, Spearman ρ_S
  - method-matched GUE 기준: -0.912 (full)
- 출력: 각 곡선의 ρ_S, δ_arith, n (영점 수), SE

**구현 지침**:
- C-279 스크립트를 복사하여 conductor/epsilon만 변경
- 43a1: `ellinit([0, 1, 1, 0, 0])` (LMFDB 기준), N=43, ε=+1, mu=[0,1]
- 61a1: `ellinit([0, -1, 1, -10, -20])` (LMFDB 11a1 isogeny... )
- **주의**: PARI의 `ellinit`에 정확한 a-invariants를 사용할 것. LMFDB에서 확인 필수.
  - 43a1: [0, 1, 1, 0, 0] 
  - 61a1: [0, -1, 1, -10, -20] — 확인 필요. 틀리면 PARI `ellsearch("43a1")` 등으로 자동 조회.
- T_MAX=500 유지 (C-282c와 동일 조건). T_MIN=2.0.
- trim 20% 양쪽 적용.
- **결과 파일**: `results/gl2_conductor_dep_c285.txt`

**모델**: sonnet
- C-279 스크립트 복사 + 파라미터 변경이므로 sonnet 충분.
- PARI a-invariants가 틀리면 sonnet이 `ellsearch`로 자동 조회하면 됨.

**왜 이것이 지금 가장 가치있는가**:
1. Paper 4의 "d-dependent damping" 주장의 **보편성** 직접 검증
2. 실험 비용 낮음 (기존 스크립트 재사용, ~30분 실행)
3. 어떤 결과든 가치: N-독립이면 보편성 강화, N-의존이면 새 연구 방향
4. B-54 경계를 직접 파고드는 실험 (경계에서 멈추지 않는다)

**성공 기준**:
- 43a1, 61a1 각각 n≥40 영점 수집
- ρ_S 수치 산출 (NaN 아님)
- δ_arith = |ρ_S| - 0.912 계산
- 4곡선(11a1, 37a1, 43a1, 61a1) δ 비교표 출력

**판정 기준**:
- 4곡선 δ의 CV(변동계수) < 25% → **양성** (N-독립, d=2 보편 감쇠)
- CV 25-50% + N과 monotone 관계 → **중립** (conductor 의존 가능성)
- CV > 50% 또는 부호 반전 → **음성** (보편성 기각)

### 우선순위

일반 연구 모드. CPU 유휴. 탐사 프로세스 없음.

### 경계 갱신

| 경계 | 상태 | 비고 |
|------|------|------|
| B-52 | 진행중 | T-limited for d≥3. GL(3)~GL(6) T 확장 한계. |
| B-53 | ★해결 | GUE_local > GUE_full. C-283. |
| B-54 | **🔬탐사중** | d-의존 감쇠 수론적 기원. C-285에서 conductor 의존성 검증. |

---

## [아카이브] 판정 + 지시 [2026-04-25 20:40 — 사이클 #286]

### C-283 확정 판정: ★★★★★ 양성 (결정적)

- GUE_local(±300, trim 20%) = -0.967 > GUE_full = -0.912
- "ζ > GUE" 주장 확정 기각. 방법론 아티팩트.
- d=1 감쇠 δ = 0.038 (비유의).

### C-284 Paper 4 개정: 완료 (7/7 기준 충족)

검토자 5개 질문 통합 판정, 통일표 9행 전수 일치 확인, 과대 표현 없음.

### 수정된 산술 감쇠 최종표

| d | L-함수 | ρ_S | GUE 기준 | δ_arith | 유의성 |
|---|--------|-----|---------|---------|--------|
| 1 | ζ(s) | -0.929 | -0.967 (local) | 0.038 | 비유의 |
| 2 | 11a1 | -0.658 | -0.912 (full) | 0.254 | ~2.4σ |
| 2 | 37a1 | -0.686 | -0.912 (full) | 0.226 | ~2.1σ |
| 3-6 | Sym^k | -0.42~-0.52 | -0.912 (full) | 0.39-0.49 | T-limited† |

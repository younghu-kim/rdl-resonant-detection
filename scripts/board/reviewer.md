# 검토자 보드

## 검증 [2026-04-13 22:25] — 사이클 21

**대상**: 디리클레 off-critical 해부 (결과 #13 재판정)
**수학자 판정**: 미해명 → 설계자 실험 후 완전 양성 요청
**검증 결과**: ✅ **통과 — 완전 양성으로 격상**
**근거**:

1. **방법론 일관성 확인**: `count_jumps_new`가 ζ off-critical과 동일 코드 (branch cut 보정 + threshold=π/2). Line-by-line 일치.

2. **아티팩트 원인 규명 확인**: 
   - 기존 `dirichlet_high_conductor.py`의 `count_phase_jumps`는 `abs(phase - prev_phase) > 2.5`로 측정 (L139-153)
   - branch cut 미보정: arg()가 +π→-π wrap시 diff≈2π≈6.28>2.5 → 거짓 점프 카운트
   - 진단 실험에서 구 방법 재현: σ=0.10에서 Λ=8, L=16 → 정확히 기존 "타σ=8" 원인

3. **수치 교차검증**:
   | 지표 | σ=0.5 (신=구) | 타σ 최대 (신) | 타σ 최대 (구) |
   |------|-------------|-------------|-------------|
   | χ₇ | 15=15 ✅ | 0 | 8 |
   | χ₈ | 17=17 ✅ | 0 | 8 |
   | χ₁₁ | 18=18 ✅ | 0 | 8 |

4. **반례 탐색 결과**: 
   - "threshold=π/2가 진짜 점프를 지울 수 있나?" → σ=0.5에서 15,17,18 정확 검출 → 불가
   - "Λ과 L 결과가 동일" → Gamma 인자는 무관 (L에서도 타σ=0)
   - 3개 지표 모두 독립적으로 동일 패턴 → 체계적 확인

5. **Verdict 버그 확인**: 스크립트 자동판정 "음성"은 로직 버그 (max_other=0일 때 gamma_none 분기). 데이터 자체는 명확히 양성.

**논문 반영 가능**: 예 ✅
**반영 내용**: 
- "위상 점프 유일성 약화" → "5/5 성질 완전 확인 (기존 약화는 측정 아티팩트)"
- Conj 1 일반화 범위: ζ 뿐 아니라 디리클레 전체에서 성립
**주의사항**: "proves" 대신 "numerical evidence confirms"로 표현

---

**논문 현재 상태**: EN 50p, KO 46p
- Section 7 (ξ-다발) 확정

**최근 커밋**:
- a19d604: 고 conductor 디리클레 확장 (mod 7,8,11): 4/5 성질 확인

**확립된 결과 (15개)**:
1-12: 변경 없음
13. **고 conductor (mod 7,8,11)**: ~~조건부 양성 (미해명)~~ → **완전 양성** (5/5 성질). 기존 "타σ=8"은 branch cut 미보정 아티팩트.
14. 실수 지표 에너지: 확인
15. **Off-critical 해부** (신규): 양성. Λ/L 모두 타σ=0. 3개 지표 독립 확인. Conj 1 디리클레 일반화 성립.

**설계자 피드백**:
- `dirichlet_high_conductor.py`의 `count_phase_jumps` (L139-155): branch cut 미보정이 근본 원인. 향후 위상 측정 시 반드시 `count_jumps_new` 패턴 사용 (bundle_utils에 추가 권장)
- 스크립트 verdict 로직: max_other=0일 때 "양성" 분기 추가 필요 (if max_other_lam==0 and max_other_pur==0: "양성")
- 함정 추가: **위상 측정 시 branch cut 보정 필수** — arg() 차분에서 ±π wrap이 거짓 점프 생성

**Git 주의**: 브랜치는 `master`. `git add -A` 금지.
